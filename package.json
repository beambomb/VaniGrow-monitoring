{
  "name": "vanigrow-bridge-glitch",
  "version": "1.0.0",
  "description": "Root package.json for Glitch deployment",
  "scripts": {
    "start": "cd bridge && npm install && node index.js"
  },
  "engines": {
    "node": "18.x"
  }
}
